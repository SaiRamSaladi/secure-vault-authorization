{
  "name": "secure-vault-authorization",
  "version": "1.0.0",
  "description": "Secure two-contract vault system with off-chain authorization",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts",
    "test": "hardhat test"
  },
  "devDependencies": {
    "hardhat": "^2.22.1",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "typescript": "^5.8.0"
  }
}
